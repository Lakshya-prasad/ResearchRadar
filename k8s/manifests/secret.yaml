apiVersion: v1
kind: Secret
metadata:
  name: researchradar-secrets
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholders before deploying.
  # In production, use Sealed Secrets, AWS Secrets Manager, or
  # inject via CI/CD pipeline — never commit real secrets to Git.
  GROQ_API_KEY: "PLACEHOLDER_KEY"
  SECRET_KEY: "PLACEHOLDER_FLASK_SECRET_KEY"
